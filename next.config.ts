// ABOUTME: Defines the Next.js application configuration for the project.
// ABOUTME: Keeps framework configuration close to the app bootstrap layer.
import type { NextConfig } from "next";

const nextConfig: NextConfig = {};

export default nextConfig;
